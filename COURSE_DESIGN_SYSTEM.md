# AI with Python · 课程设计系统

仅修改 AI with Python 课件时读取。本文补充 [AGENTS.md](AGENTS.md) 的系列规范；首页与目录见 [入口规范](docs/COURSE_ENTRY_SPEC.md)。通用教学与验收要求只在 AGENTS.md 维护。

## 定位与实现入口

- 面向小学五年级至初中一年级，以教师逐页投屏为主；课程名为 AI with Python，“AI 探索局”是课堂体验概念。
- 主色为 Ink Navy `#071A3D`、Atlas Cobalt `#1457D9`、Signal Yellow `#FFC91C`、Paper Cream `#F5F0E6`；Coral `#F36D4A` 用于少量冲突或提示。
- 共享标题与 `--lesson-page-*` 变量在 `app/globals.css`；`Stage`、练习模板与印章在 `app/courses/ai-with-python/lesson-01/lesson-ui.tsx`；`ChapterCover` 当前在同目录 `lesson-original-scenes.tsx`。复用前确认实际导出，必要时提取共享组件，不复制页面骨架。
- 下列视觉数值是系列默认值。为可读性或明确设计目标调整时，统一修改对应共享样式并验证受影响页面；单屏演示、状态保留与第一课约定仍为产品要求。

### 视觉与演示

- 保留深蓝、蓝、黄色及纸色，以及数据探索图鉴、书脊、编号与档案印章的视觉语言。
- 只提供逐页演示，不引入阅读模式或小窗口自动长页；保留全屏、键盘翻页与书脊全局操作。翻页保留本轮答案、揭晓结果和步骤，区分“重置本页互动”与“重新开始本课”。
- 页面保留稳定安全边距（左右 **4–6cqw**、上下 **3–5cqh**），主体不依赖滚动；优先单栏或双栏，双栏通常采用 **40/60 或 50/50**。普通卡片使用 2px 边框，静态卡片不设常驻阴影；交互卡片仅在悬停、聚焦或选中时显示错位阴影。

### 标题与模板

- 标题只分封面与普通教学页两级。普通页统一使用 `.lesson-standard-heading` 和共享变量：标签 **18px**、内边距 **8px 12px**，主标题 **clamp(48px, 4.2cqw, 68px)**、最多两行；标签与标题间隔 **12px**，标题组与正文间隔 **16–24px**。其余字重、字距和配色沿用共享样式，不做单页覆盖。
- 标签识别页面类型或任务阶段，主标题表达唯一重点；标题组占满内容宽度，不加底部横线或重复副标题。
- 章节封面复用 `ChapterCover`：深蓝网格、左标题右章节号，右侧留安全距离；主标题 **72–108px**、最多两行，底部仅保留一至两行“本章任务”。
- 课后练习复用 `CheckpointTaskTemplate`：左侧任务说明、右侧练习内容；标签为 `CHECKPOINT · 课后练习 NN`，左侧深蓝任务区保留黄色 `CHECKPOINT NN` 印章与行动标签。任务问题、完成指令和辅助提示清楚，套用模板不得丢失内容或状态。
- 课堂练习复用 `ClassPracticeStamp` 并传入 `checklist`：标签为 `CLASS PRACTICE · 课堂练习 NN`，仅入口展示带清单图标的黄色 `CHECKLIST NN` 圆章；后续页沿用标签和连续步骤条，保持标题、工作区及操作对齐。课堂与课后练习印章均在进入对应页面时播放一次短促盖章动效。

### 代码与小派讲解

- JupyterLab 代码区沿用第一课确认的浅色风格：笔记本标签、蓝色选中边框、代码单元格和语法配色。默认左侧代码、右侧讲解；按内容调整栏宽，保证完整网址与关键语句清晰可读。优先复用 `lesson-02/notebook-panels.tsx` 中的 `NotebookPanel`，展示代码和复制内容保持一致。
- 小派参与解释当前代码，用简短气泡、关键词强调和具体图示建立对应关系。根据内容变化角色位置、气泡方向、动作和图示组合，避免每页固定为同一张角色站图加大段文字；参考第一课课堂练习三及 `lesson-01/xiaopai-speech.tsx` 的 `XiaopaiSpeech`。动画控制遵循通用规范。
- 分步讲解时同步更新代码高亮与小派的说明；已经出现的关键代码保持可见。讲解概念可配合故事中的工具、地址、信件等形象，但须点明它们对应的代码或技术含义。

### 第一课约定

- 学生已掌握 Python 变量、列表、循环及函数调用，时长由教师灵活安排。
- 未来世界创作保持开放，五幅参考作品同时可见，不要求统一的 AI 功能、输入输出说明或标准答案。
