# 课程项目协作指南

## 目标与规则边界

交付适合课堂投屏、概念准确、学生能理解并操作的课程。以用户当前任务与验收结果为目标，自主选择实现方法。

- 在系统、开发者及工具权限约束内，用户当前明确要求优先于仓库文档和技能中的默认建议。已确认的授权和偏好持续有效；设计默认值不构成额外审批要求。
- 本文件维护通用规则；修改首页或目录时读取 [入口规范](docs/COURSE_ENTRY_SPEC.md)，修改 AI with Python 课件时读取 [系列规范](COURSE_DESIGN_SYSTEM.md)。仅加载相关文档；`design-reviews/` 是历史依据，不能覆盖当前要求。
- 教学准确性、可读性和明确的产品行为是底线。布局、比例、装饰与样式数值是默认值，可为任务目标合理调整并验证；系列样式按容器、`data-course-series` 或 CSS Module 隔离。

## 工作方式

- 行动请求持续推进到实现与适用验证完成；常规缺省项结合上下文自行判断。只在缺失信息会实质改变结果且无法合理推断时提问，同时推进不依赖答案的工作。
- 需要额外授权时，先完成已授权的准备工作，给出可审阅的具体结果。若文件指令导致暂停，指出文件、原文及适用原因，不把自己的推断当成强制要求。
- 先定位相关代码、素材与已有实现。视觉任务实际查看参考图和页面；复用依赖、组件、变量与数据来源，保持链接稳定，保护已有未提交改动，清理本次相关重复实现。
- 复杂任务按可验证的阶段推进；新消息用于修正当前工作，保留仍然有效的目标与已完成结果。简单修改直接完成。
- 运行环境允许时，对可独立进行的调研、实现或审查使用有明确范围的子代理；避免多人修改同一文件，由主代理整合验证。小任务无需拆分。
- 默认用简洁中文沟通，先讲结果；交付说明改动、实际验证与剩余限制，不重复过程或夸大完成度。

## 通用教学技巧

1. **能展示，就少描述。** 知识引入和概念讲解优先用图片、实物、对比和演示承载核心信息，文字点明关键。图片应解释情境、关系或过程，而非仅装饰文字；按展示区域选择横竖构图，图文结合，避免大块无效留白。
2. **先具体，后抽象。** 从熟悉的经验和可观察的现象出发，先建立需求，再引出概念、术语和工作过程，最后进入代码。故事中的困难应能说明技术为什么有用，不提前用尚未解释的术语推进故事或任务。
3. **一次聚焦一个重点。** 拆分复杂内容，逐步呈现，让学生始终知道当前要理解什么。
4. **让知识形成连续的线索。** 新知识承接已有理解，减少无关的情境切换，逐步增加难度。前后环节尽量沿用同一故事、样本和任务成果，让上一环节的产出成为下一环节的输入。
5. **先让学生想，再给答案。** 留出观察、猜测和尝试的空间，通过结果与反馈推动理解。
6. **用变化检验理解。** 改变条件、例子或任务，检验学生能否解释和应用；创作任务保留多种可能。

## 代码讲解与课堂练习

- 代码展示贴近学生实际使用的编程环境；AI with Python 默认采用 **JupyterLab 样式**，由**小派结合图示作多样化讲解**，具体呈现遵循系列规范。
- 代码保持适合跟写的自然格式，用空行区分逻辑段。完整网址等内容优先保持一行；不为适应窄栏引入额外的字符串拼接或不必要的语法，先调整布局。
- 每次练习聚焦当前已学知识，明确学生要完成的动作及可观察的完成标准。跟写练习展示完整代码，让学生亲手输入、运行和核对，不以执行现成脚本替代跟写任务。
- 交互服务于观察、尝试和核对；适合直接展示的内容不强行改成选择题。分步标签切换时，图示、高亮和说明应同步对应当前步骤。
- 完整源码、输出对照等较长材料可按需放入弹窗，方便教师核对；优先从实际教学样本读取，避免与展示页面维护两份不一致的数据。主页面保留当前任务和关键证据。
- 学生可见页面与教学样本保持简洁，不添加无关版本号、虚构声明或开发说明；必要的运行条件、输出示例及节选标识仍须保留。

## 课程质量底线

- 术语准确、适龄；类比与模拟标明范围。代码提供运行条件、执行顺序、预期输出及可复制的完整版本，节选须标明；演示结果不得伪装成现场执行或训练。
- 揭晓后保留原题、学生选择和关键证据，支持重试。操作练习明确完成标准，开放创作提供多种思路，通关文案只描述实际考查的能力。
- 课堂与课后练习每课各自从 01 编号；跨页沿用编号，步骤条区分环节，标签与印章同步。课堂练习入口统一使用带清单图标的 **CHECKLIST NN** 标记；课后练习使用 **CHECKPOINT NN**，不混用。
- 教学过程支持分步、暂停与重播；视频和循环动效由教师控制、离页停止，尊重减少动态效果。首页自动行走按入口规范的明确例外执行。
- 教学可见文字计算字号最低 **18px**，涵盖代码、按钮、反馈、弹窗、图表及 SVG；图片关键内容也须清晰，必要时放大并提供文本对照。字号检查例外仅限书脊品牌、全局导航、进度页码与仅供屏幕阅读器使用的隐藏文字。
- 正文默认 **24–28px**、代码 **20–24px**，正文中等字重、约 1.5 倍行距。当前步骤的题目、数据、代码、依据与操作完整可见；空间不足就精简、分步或拆页。
- 同组内容对齐，保持对比度与安全边距，装饰不干扰内容。固定组件间距默认 **8／12／16／24／32px**，双栏默认 **24px**。

## 项目导航与命令

- 路由与课件：`app/`；共享导航：`components/course/`；课程数据：`lib/course-data.ts`；素材：`public/courses/`。
- Node.js **≥22.13**，使用 npm；依赖与脚本以 `package.json` 为准。开发启动 `npm run dev`，访问终端给出的地址。
- 构建：`npm run build`；代码检查：`npm run lint`；格式检查：`npm run format -- --check <本次修改文件>`。限定代码检查范围时用 `npm run lint -- <代码文件>`；按需用 `npx --no-install tsc --noEmit` 检查类型。

## 完成与验证

- **仅文档**：检查内容一致性、引用和格式；无需构建或浏览器验收。
- **代码或样式**：运行构建、相关代码与格式检查；有行为变化时验证真实操作与结果。只增加能捕获实际回归的测试。
- **页面、交互或教学素材**：在实际浏览器视口 **1280×720、1366×768、1920×1080，100% 缩放**检查受影响页面及适用的初始、选择、展开、正确、错误、重置和最长反馈状态。文字、树节点、浮层、阴影及焦点轮廓无重叠裁切；结合截图与实际操作判断。
- **首页、目录**：另查窄屏无横向溢出、阶段定位、键盘聚焦、图片弹窗和往返链接；共享组件变更覆盖受影响入口与页面类型。
- 所需检查通过后即可交付；仅有新修改、失败或未解决疑点时扩大或重复验证。遇到既有错误区分本次影响，报告未完成检查及原因，不顺带开展无关重写。

维护本文件时，只固化稳定偏好、项目约束和反复出现的问题；细节在对应规范或代码中维护，避免重复规则。
