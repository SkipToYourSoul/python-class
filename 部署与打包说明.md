# 课程项目：迁移与打包

## Git 仓库与可选源码包

在另一台电脑继续开发，优先克隆仓库并安装锁定依赖：

```bash
git clone https://github.com/SkipToYourSoul/python-class.git
cd python-class
npm ci
npm run dev
```

如需离线传递项目文件，运行下方打包命令生成 `release/ai-with-python-source.zip`。解压后得到 `ai-with-python-course/`，包含课程源代码、全部公开资源、依赖锁文件和构建配置；安装依赖仍需联网或已有 npm 缓存。

它不是双击 HTML 即可打开的静态网站，也不是已经适配所有服务器的生产镜像。当前项目采用 vinext + Vite + Cloudflare Workers 插件。`npm start` 实际执行 Wrangler 的本地预览命令，不是常驻生产服务器命令。

## 打包时保留与排除

保留：

- `app/`、`components/`、`hooks/`、`lib/`：页面、组件与课程数据。
- `public/`：课程图片、视频、字体等资源；完整保留，避免遗漏动态引用的文件。
- `package.json`、`package-lock.json`：依赖及版本锁定。
- `vite.config.ts`、`next.config.ts`、`tsconfig.json` 及辅助配置；`next-env.d.ts` 由开发工具生成，不提交或打包。
- `.openai/hosting.json`：当前 Vite 配置直接导入此文件，因此源码包必须带上。
- README、本说明、macOS 启动脚本和重新打包脚本。
- `AGENTS.md`、`COURSE_DESIGN_SYSTEM.md`、`docs/COURSE_ENTRY_SPEC.md` 和规范引用的 `design-reviews/course-home-reference.png`：继续开发所需的规则与参考图。

排除：

| 目录/文件                          | 原因                                                             |
| ---------------------------------- | ---------------------------------------------------------------- |
| `node_modules/`                    | 在目标环境重新安装；含操作系统相关的原生依赖，不应直接跨系统复制 |
| `dist/`                            | 构建产物，在新环境重新构建；其中还复制了一份公开资源             |
| `.git/`                            | Git 历史，与运行无关；源码包不作为版本历史备份                   |
| 历史评审、实现记录、`output/`      | 生成草稿与旧记录；当前规范及其引用的参考图仍保留                 |
| `.next/`、`.vinext/`、`.wrangler/` | 缓存和本地工具状态                                               |
| `*.tsbuildinfo`、日志、`.DS_Store` | 增量构建及系统文件                                               |
| `.env*`                            | 环境变量文件，应在目标环境单独配置                               |
| `release/`                         | 避免把已有压缩包再次打包                                         |

`package-lock.json`、`vite.config.ts` 等必需文件均纳入 Git。打包脚本使用明确的文件清单，不依赖 Git 状态；`release/` 只保存本地产物，不提交到仓库。教学页面使用的 `public/` 内 ZIP 练习包需保留，不能一概忽略所有 ZIP 文件。

## 在另一台电脑验证

安装满足 `package.json` 要求的 Node.js（当前至少 22.13.0），在解压目录执行：

```bash
npm ci
npm run build
npm run dev -- --port 3000 --strictPort
```

打开 `http://localhost:3000`。最后一条仅用于本地开发/验收，按 Ctrl+C 关闭。`npm ci` 需要联网访问 npm registry，并安装构建所需的开发依赖；不要在构建前加 `--omit=dev`。首次安装前不要手动删除锁文件。

macOS 也可使用保留的 `启动课程网站.command`，但可复现安装优先用上面的 `npm ci`。

## 部署到其他环境

- **另一台电脑本地运行**：使用上面的流程即可。
- **Cloudflare Workers**：需要配置目标账户和 Worker 的部署配置。包中现有 `.openai/hosting.json` 是原 Sites 项目的关联信息，不会自动绑定到新的 Cloudflare 账户。
- **普通 Linux 服务器或 Docker**：需要先将当前 Cloudflare 构建入口适配为 Node 运行目标，再生成、测试对应生产启动文件；不能直接把当前 `dist/client/` 当成完整网站上传，也不能把现有 `npm start` 当作已完成生产部署。
- **纯静态托管**：需要另行验证静态导出，包括所有页面和互动游戏路由；当前包未声称提供已验证的纯静态产物。

当前课程无账号、数据库或业务接口，学习进度/答案保存在浏览器中。源码包不包含浏览器中的个人进度；换浏览器或域名后不会自动迁移这些状态。

框架的运行目标和独立 Node 输出能力可参见 [vinext 官方说明](https://github.com/cloudflare/vinext#readme)。本包保留项目已验证的配置，未直接切换运行目标。

## 重新打包

修改课程后，在项目根目录运行（需要 Python 3）：

```bash
python3 scripts/package-source.py
```

输出 ZIP 与 SHA-256 校验文件。脚本逐个校验压缩成员与源文件内容一致，不修改或清理原项目文件，也不会启动或发布网站。

## 验证范围

源码包检查包含全部 `public/` 资源与必需配置，并验证 ZIP CRC 和每个文件的 SHA-256。第二课完成后的交付包含两课全部页面、游戏、教学样本及练习包。2026-09-09 将压缩包解压至独立临时目录，复用本机已有 `node_modules` 执行 `npm run build`，构建成功，没有启动服务。在实际目标操作系统上的全新依赖安装和生产部署仍须按所选目标验证。
